# Copy this file to .env (local) or set these as real environment variables
# on the server (production). Never commit the filled-in version.

APP_ENV=local

DB_HOST=localhost
DB_PORT=5432
DB_NAME=browave_ams
DB_USER=postgres
DB_PASS=changeme
